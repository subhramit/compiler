_simpleInterest input parameter list[real principal, real rate, real time] output parameter list [real interest];
principal <--- 5000.0;
rate <--- 0.05;
time <--- 2.5;
interest <--- (principal * rate * time);
return [interest];
end

_main
type real: totalInterest;
totalInterest <--- call _simpleInterest with parameters [1.1,2.2,3.3];
write(totalInterest);
end
